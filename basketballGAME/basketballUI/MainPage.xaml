<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="basketballUI.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Maui api demo"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="look at the xaml.cs to look at what your adding"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="look at the xaml.cs to look at what your adding" />

            <Button
                x:Name="CounterBtn"
                Text="Get all" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="CounterBtn2"
                Text="Post" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="CounterBtn2_Clicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="CounterBtn3"
                Text="Put " 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="CounterBtn3_Clicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="CounterBtn4"
                Text="get at specific location" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="CounterBtn4_Clicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="CounterBtn5"
                Text="Delete" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="CounterBtn5_Clicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
